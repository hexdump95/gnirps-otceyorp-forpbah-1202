spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/db_nuevo2
    username: admin
    password: secret
    driverClassName: org.postgresql.Driver
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: create-drop
